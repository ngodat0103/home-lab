# Database Configuration
VAULTWARDEN_DB_HOST=127.0.0.1
VAULTWARDEN_DB_PORT=5432
VAULTWARDEN_DB_NAME=vaultwarden
VAULTWARDEN_DB_USERNAME=vaultwarden
VAULTWARDEN_DB_PASSWORD=bestPasswordInTheWorld


# S3 Configuration
S3_BUCKET=backup-storage-prod
S3_ENDPOINT=https://4c
AWS_ACCESS_KEY_ID=485508
AWS_SECRET_ACCESS_KEY=60
AWS_REGION=auto
# Backup Configuration
BACKUP_PASSWORD=A
LOCAL_VAULTWARDEN_DATA_DIR=/mnt/data2/docker/vaultwarden_data
VAULTWARDEN_DATA_DIR=/app/data/vaultwarden/
LOCAL_BACKUP_DIR=/root/vaultwarden-backup

# Remote SSH Configuration (for --mode=remote disaster recovery)
# Uncomment and configure these if you're using remote mode to restore to a VM
#SSH_HOST=192.168.1.100
#SSH_PORT=22
#SSH_USER=root
#SSH_KEY_PATH=/home/user/.ssh/id_rsa
# OR use password authentication (less secure):
#SSH_PASSWORD=your-ssh-password

# Remote paths (for --mode=remote)
#REMOTE_VAULTWARDEN_DATA_DIR=/opt/vaultwarden/data
#REMOTE_TEMP_DIR=/tmp/vaultwarden_restore
#REMOTE_SYSTEMD_SERVICE=vaultwarden.service

# Timeouts (optional)
#PGRESTORE_TIMEOUT=600
#SSH_TIMEOUT=30